<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/profile.css" />
  </head>
  <body>
    <%- include("partials/nav") %>

    <div class="container">
      <section class="left">
        <h2>我的帳號</h2>
        <hr />
        <div class="sideMenu">
          <ul>
            <li><a href="#">會員中心</a></li>
            <li><a href="#">優惠券</a></li>
            <li><a href="/auth/change-password">更改資料</a></li>
            <li><a href="/auth/logout">登出</a></li>
          </ul>
        </div>
      </section>

      <section class="right">
        <div class="thumbnail">
          <img src="<%=user.thumbnail %>" alt="image" />
          <h4><%=user.name %></h4>
        </div>

        <p>
          <%=user.name %> 您好 (不是 <%=user.name %> 嗎？ 請<a
            href="/auth/logout"
            >登出</a
          >)
        </p>
        <div class="memberLevel">
          <p>目前擁有的會員福利:</p>
          <button>普通會員</button>
        </div>
        <div class="info">
          <p>姓名: <%=user.name %></p>
          <p>加入會員時間: <%=user.date %></p>
          <p>Email: <%=user.email %></p>
          <% if(user.googleID){%>
          <p>Google ID: <%=user.googleID %></p>
          <%}%> <% if(user.googleID){%>
          <p>Line ID: <%=user.lineID %></p>
          <%}%>
        </div>
      </section>
    </div>

    <%- include("partials/footer") %>
  </body>
</html>
